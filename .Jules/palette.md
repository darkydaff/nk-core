## 2024-05-20 - Focus indicators for icon buttons
**Learning:** Icon buttons missing accessible focus indicators (`focus:ring`) and proper `aria-label`/`aria-hidden` setups makes keyboard navigation nearly impossible in forms and lists.
**Action:** Update buttons in lists/grids to include standard aria-label attributes and hidden icons.
